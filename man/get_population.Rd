% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_pep.R
\name{get_population}
\alias{get_population}
\title{Get Census Bureau population estimates}
\usage{
get_population(
  geography = c("county", "state", "nation"),
  years = NULL,
  geoids = NULL,
  variables = NULL,
  vintage = "latest"
)
}
\arguments{
\item{geography}{Character. Geographic level to return: \code{"all"}, \code{"county"},
\code{"state"}, or \code{"nation"}. Ignored when \code{geoids} is provided. Default: \code{"all"}.}

\item{years}{Integer vector. Years to return. Default: all available.}

\item{geoids}{Character vector. FIPS codes to return (5-digit county,
2-digit state, or \code{"00"} for national). When provided, takes precedence
over \code{geography}. Default: \code{NULL} (all geographies).}

\item{variables}{Character vector. Variables to return: \code{"population"} and/or
\code{"population_16plus"}. Default: both.}

\item{vintage}{Character. Vintage to read, e.g. \code{"2023"}. Default: \code{"latest"}.}
}
\value{
A data frame with columns: \code{geoid}, \code{year}, \code{variable}, \code{value}.
}
\description{
Returns total population and working-age population (16+) from the U.S.
Census Bureau's Population Estimates Program (PEP). Data cover 2000–present
at annual frequency for county, state, and national geographies.
}
\examples{
\dontrun{
# All county-level population
get_population(geography = "county", years = 2010:2023)

# Specific county — Grafton County, NH
get_population(geoids = "33009", years = 2010:2023)

# Working-age population only, state level
get_population(geography = "state", variables = "population_16plus", years = 2015:2023)
}

}
\seealso{
\code{\link[=get_population_change]{get_population_change()}}, \code{\link[=get_pep_codebook]{get_pep_codebook()}}
}
