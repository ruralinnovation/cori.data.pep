% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_pep.R
\name{get_population_change}
\alias{get_population_change}
\title{Get Census Bureau population change components}
\usage{
get_population_change(
  geography = c("county", "state", "nation"),
  years = NULL,
  geoids = NULL,
  variables = NULL,
  vintage = "latest"
)
}
\arguments{
\item{geography}{Character. Geographic level to return: \code{"all"}, \code{"county"},
\code{"state"}, or \code{"nation"}. Ignored when \code{geoids} is provided. Default: \code{"all"}.}

\item{years}{Integer vector. Years to return. Default: all available.}

\item{geoids}{Character vector. FIPS codes to return (5-digit county,
2-digit state, or \code{"00"} for national). When provided, takes precedence
over \code{geography}. Default: \code{NULL} (all geographies).}

\item{variables}{Character vector. Variables to return. Options:
\code{"births"}, \code{"deaths"}, \code{"natural_change"}, \code{"domestic_migration"},
\code{"international_migration"}, \code{"net_migration"}. Default: all.}

\item{vintage}{Character. Vintage to read, e.g. \code{"2023"}. Default: \code{"latest"}.}
}
\value{
A data frame with columns: \code{geoid}, \code{year}, \code{variable}, \code{value}.
}
\description{
Returns components of population change — births, deaths, natural change,
and migration — from the U.S. Census Bureau's Population Estimates Program
(PEP). Data cover 2000–present at annual frequency for county, state, and
national geographies.
}
\examples{
\dontrun{
# All components, county level
get_population_change(geography = "county", years = 2010:2023)

# Net migration only — Grafton County, NH
get_population_change(geoids = "33009", variables = "net_migration")

# Migration components, state level
get_population_change(
  geography = "state",
  variables = c("domestic_migration", "net_migration"),
  years     = 2015:2023
)
}

}
\seealso{
\code{\link[=get_population]{get_population()}}, \code{\link[=get_pep_codebook]{get_pep_codebook()}}
}
