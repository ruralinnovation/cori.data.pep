% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_pep_from_s3.R
\name{read_pep_from_s3}
\alias{read_pep_from_s3}
\title{Read processed PEP data from S3}
\usage{
read_pep_from_s3(
  vintage = "latest",
  variables = NULL,
  years = NULL,
  geoids = NULL,
  s3_bucket = "cori.data.pep",
  s3_path_prefix = ""
)
}
\arguments{
\item{vintage}{Character. Vintage to read, e.g. \code{"2023"}. Default: \code{"latest"},
which reads the \verb{_LATEST} pointer written by \code{\link[=write_pep_processed_to_s3]{write_pep_processed_to_s3()}}.}

\item{variables}{Character vector. Variables to return. Default: all.
See \code{\link[=get_pep_codebook]{get_pep_codebook()}} for names.}

\item{years}{Integer vector. Years to return. Default: all.}

\item{geoids}{Character vector. 5-digit county FIPS codes to return.
Default: all.}

\item{s3_bucket}{Character. S3 bucket name. Default: \code{"cori.data.pep"}.}

\item{s3_path_prefix}{Character. Optional prefix matching the one used in
\code{\link[=write_pep_processed_to_s3]{write_pep_processed_to_s3()}}, e.g. \code{"test/"}. Default: \code{""}.}
}
\value{
A data frame with columns: \code{geoid}, \code{year}, \code{variable}, \code{value},
\code{agg_var}.
}
\description{
Queries CORI's processed Population Estimates Program parquet files from S3
using DuckDB. Returns long-format data — one row per geoid/year/variable.
}
\examples{
\dontrun{
# All data, latest vintage
df <- read_pep_from_s3()

# Total population, specific years
df <- read_pep_from_s3(variables = "population", years = 2010:2023)

# Migration components for specific counties
df <- read_pep_from_s3(
  variables = c("domestic_mig", "net_mig"),
  geoids    = c("54011", "54025")
)
}

}
\seealso{
\code{\link[=latest_pep_vintage]{latest_pep_vintage()}}, \code{\link[=get_pep_codebook]{get_pep_codebook()}}
}
